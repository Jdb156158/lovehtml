# 2026-08-19 工作记录

## 从 GitHub 取材新增 3 个七夕爱心特效（qixi-18/19/20）
用户要求"从 github 找别人分享好一点的做出来"。用 WebSearch + WebFetch（raw.githubusercontent.com，走代理）扒到 3 份纯前端源码并复刻成离线单文件，全部经 vm 模拟 canvas 真跑验证（12 帧无错、零 shadowBlur）：

- **qixi-18-flow-heart.html** ← luca-petre/heart-animation（main/heart.js，45 commits 成熟）。粒子流光带（v=34 条，每条 34 粒子拖尾）缠绕汇聚成心 + 心跳脉动（beat=1+0.07*sin）。颜色 HSL 300~360 粉紫，globalCompositeOperation='lighter' 发光，clearRect 清屏（无 rgba 拖影）。原版是 rgba(0,0,0,.2) 拖影，改 lighter 更干净。
- **qixi-19-love-message.html** ← Lucky-Joshi/Love-Site（main/index.html）。发光大心（粒子轨迹向心形汇聚 + pulse 脉动）+ 上升小爱心（radial gradient 小爱心）+ 顶部 "I love you" CSS 跳动变色文字。去掉了原版 Google Fonts CDN 依赖，改本地 cursive 字体；去掉 canvas 中心重复 drawText，只留 CSS 文字。
- **qixi-20-draw-heart.html** ← GunalHincal/code-of-love（main/index.html）。互动：点"开始"→ 实时逐步画红心(stroke) → 自实现烟花粒子爆发(去掉原 canvas-confetti CDN，用 lighter 粒子) → 满屏小爱心覆盖层 → 点"擦除"拖动擦掉表层(destination-out) 露出底层 lighter 发光 "I LOVE YOU" 心。去掉原 music.mp3 外部音频。心里话用 MSG_LINE1/MSG_LINE2 常量可改。

## 已验证做法
- 三个文件均：`window.onerror` 错误兜底（红字提示）+ 循环内零 shadowBlur + lighter 叠加发光 + 响应式 resize。
- 验证脚本用 vm + canvas/DOM 桩驱动 requestAnimationFrame 多帧，捕捉 TDZ/未定义/NaN 类运行时错误（比 new Function 语法校验更可靠）。
- index.html 导航页已更新：计数 17→20，末尾插入 3 张新卡片。

## 注意
- islacollins/AnimatedGlowingHeart 实际源码只是个静态渐变填充心、无任何动画，已放弃。
- raw 直连有时 404（分支/路径错），需先用 WebFetch 试 main 再试 master，或看 repo 页面确认真实文件名。
- 仓库目录现有 qixi-1~20 共 20 个特效 + index.html 导航页 + README.md + 七夕爱心效果(17个效果).zip（旧打包）。

## 用户要求"再多做一些" → 新增 qixi-21~25（共 5 个，纯手写、未取自 GitHub）
全部沿用质量基线：离线单文件 / 循环内零 shadowBlur / globalCompositeOperation='lighter' 发光 / window.onerror 红字兜底 / 响应式 resize。经 vm 模拟 canvas 真跑 30 帧验证（无错、零 NaN）：
- **qixi-21-heart-halo.html**（心跳光环）：实心径向渐变粒子心 + 旋转光环 + 上升小爱心 + 点击绽放星火。
- **qixi-22-firework-heart.html**（烟花心爆）：星点夜空 + 烟花升空，爆开瞬间化作爱心形状闪烁消散（trail 用 rgba 半透明清屏而非 shadowBlur）。
- **qixi-23-heart-bubbles.html**（爱心气泡）：大小不一的爱心气泡从底部上升、轻摆，外圈柔光（lighter 描边/填充双层）。
- **qixi-24-twin-hearts.html**（双心对称）：左右镜像两心，粒子沿轮廓流动 + 中间心跳发光 + 尖端之间流动星点。
- **qixi-25-ecg-heart.html**（心电图心跳）：滚动 ECG 波形（PQRST 用高斯叠加）+ 每次 R 峰脉冲绽放一颗爱心随波形左移。
index.html 导航页计数 20→25，末尾插入 5 张新卡片。

## 用户"再找一些好看的，多找点" → 新增 qixi-26~30（共 5 个，多为 GitHub 取材复刻）
经 WebSearch 找候选 + WebFetch 抓源码，复刻为离线单文件，全部过 vm 校验（30 帧无错、零 NaN；qixi-27 纯 CSS 故 0 rAF 帧但脚本无报错）：
- **qixi-26-3d-heart-show.html**（3D 粒子爱心烟花秀）← 复刻 [lin1392519535/love_heart]。2600 粒子填满 3D 爱心方程 + 星河 + 爱心流星雨 + 点击炸开星星/爱心 + 自动旋转/心跳。去 Web Audio 音频、改响应式（DPR 缩放）、循环内零 shadowBlur（原版 falling/click 用 shadowBlur，已移除）。
- **qixi-27-love-letter-card.html**（浪漫情书卡片）← 复刻 [kingoftech-v01/valentine]。纯 CSS：漂浮爱心、跳动大心、花瓣、开门相拥动画、点击撒心。去掉 Google Fonts 外链换系统字体（KaiTi/Songti/serif），法文文案中文化。完全离线。
- **qixi-28-rainbow-hearts.html**（多彩漂浮爱心）：彩虹色爱心上升+轻摆+柔光（lighter 双层），治愈系，区别于 qixi-23 单色气泡。
- **qixi-29-love-you-text.html**（粒子文字 I LOVE YOU）：离屏 canvas 采样文字像素→粒子目标点，汇聚/停留/炸散/重组循环。
- **qixi-30-neon-heart.html**（霓虹描边心跳）：描边爱心 + 跑马灯流光沿轮廓奔跑 + 心跳脉动 + 中心辉光。
index.html 导航页计数 25→30，插入 5 张新卡片。

### 复刻 GitHub 源码的经验
- raw.githubusercontent.com 直连常被沙箱拦（status 000）→ 优先 WebFetch 直接抓 raw URL，多数能返回完整源码。
- 复刻时务必：① 去外部依赖（Google Fonts/CDN/外链 JS）；② DPR 适配改响应式；③ 循环内 shadowBlur 全部移除（原版少量元素用 shadowBlur 发光，改用 lighter/双层描边）；④ 音频类按需删除。

## 更新 README.md 至 v3 完整版
用户要求补全 README 并展示部分好看特效。已将 2 行占位文档重写为结构化中文 README：快速开始、精选推荐表（7 个）、30 个特效按 6 类（心跳经典/粒子文字/爱心雨漂浮/烟花爆发/浪漫互动/科技几何）分组清单、目录结构、质量基线、自定义方法、更新日志（v1/v2/v3）。所有文件名与 <title> 已逐一 Grep 核对确保准确，无虚构。

## 部署到 GitHub Pages（2026-08-19 晚）
用户想把导航页部署到 github.io 让任何人可访问。完成情况：
- 仓库已连通 `origin`：`git@github.com:Jdb156158/lovehtml.git`（原 https 地址，已切 SSH），分支 `main`，较早的 `df77527 "特效更新"` 已含 30 特效。
- **修复 .git 污染**：移动硬盘上 macOS 自动生成 `._` 垃圾（AppleDouble 资源分叉）污染 `.git/objects/pack` 与已跟踪文件，导致 git 报 `non-monotonic index`。清理 `.git` 内 `._` + `git rm --cached` 已跟踪 `._*` + 加 `.gitignore`（`._*`、`.workbuddy/`、`*.zip`）防再生。
- **隐私修复**：早前误提交的 `.workbuddy/memory/2026-08-13.md`、`2026-08-19.md` 和旧打包 `七夕爱心效果(17个效果).zip` 已从版本库移除跟踪（`git rm --cached -r .workbuddy` 仅取消跟踪、本地物理文件保留），重新 push。GitHub API 复核远程树已无此类文件。
- **SSH 认证**：`~/.ssh` 受 macOS 保护无法写入（`Operation not permitted`），密钥生成于 `/Users/jiao/lovehtml_deploy_key`（私钥本机、未外传），推送用 `GIT_SSH_COMMAND="ssh -i /Users/jiao/lovehtml_deploy_key"`。公钥 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIGUqt5Rs/1NQ+REDT/qEaio8qTVUlf7D5L8Pe1O+6o51 lovehtml-deploy` 已让用户加到 GitHub → Settings → SSH and GPG keys。
- 已 push 至 `main`（`df77527..cb49764`），经 GitHub API 验证远程含 30 特效 + index.html + README.md，无泄露。
- **待用户手动完成**：GitHub 仓库 Settings → Pages → Source 选 `main` 分支 /(root) → Save，约 1–2 分钟后访问 `https://Jdb156158.github.io/lovehtml/`。SSH 密钥只能用于 git 推送，无法直接调 GitHub 设置 API 开 Pages，故这一步需网页操作。
- 注：GitHub Pages 部署出的站点为公开可访问，无论源仓库 public/private；本仓库经 API 未认证可读，确认为 public。

## 修正：.workbuddy 工作日志应纳入版本库（同上晚些时候）
用户澄清：换电脑仍需这些工作日志，且希望别人拿到源码也带记忆文件。故推翻上条"移除 .workbuddy 防泄露"的判断。已执行：① 清理 `.workbuddy` 内 `._` 垃圾；② `.gitignore` 去掉 `.workbuddy/` 忽略规则（保留 `._*`、`*.zip`）；③ `git add .workbuddy` + commit + push（`cb49764..0d63d4a`）。经 GitHub API 复核，远程现有 `.workbuddy/memory/2026-08-13.md`、`2026-08-19.md` 两个工作日志。
**结论**：`.workbuddy/memory/` 视为可随仓库同步/分享的项目记忆，不再当作隐私隐藏；`.gitignore` 仅继续忽略 `._*`（AppleDouble 垃圾）与 `*.zip`（旧打包）。
